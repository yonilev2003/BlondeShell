# BlondeShell — Session Progress
## Date: 2026-04-18 EOD | Status: v5.2 + Sprint 1 DONE + Night Session DONE — Sprint 2 LOADED

---

## CURRENT BRANCH
Working branch: `claude/push-to-github-PjFnX` (per session guidance).
`main` has v5.2 merged from earlier today. EOD commits on this branch will be merged to main after user review.

## FIRST ACTION IN MORNING SESSION (Apr 19)
1. Read `MORNING_QUESTIONS.md` — Part A verification + locked Part B decisions.
2. Start Sprint 2 day work — plan in this file below.

---

## v5.2 STATE (as of 2026-04-18 EOD)

### ✅ COMPLETED THIS SESSION

**Architecture upgrades:**
- `lib/ab_testing.js` — A/B test engine (5 caption variations, 30min stagger, pick winner after 2h)
- `lib/hook_database.js` — hook performance tracking by platform + type
- `agents/tool_eval_agent.js` — monthly tool/stack evaluation → Obsidian report
- `agents/coo_agent.js` v2 — week-over-week, variety score, platform health, learning quality, daily email always sent
- `webhook/server.js` — viral check every 30min (was 2h), tool_eval + pipeline crons added
- `lib/pipeline.js` — integrates A/B testing for T1 TikTok/Instagram

**Hedra → Kling v3 lipsync refactor:**
- `lib/hedra.js` DELETED
- `lib/lipsync.js` NEW — uses `fal-ai/kling-video/lipsync/audio-to-video`
- `generateTalkingHead({ startImageUrl, audioUrl, prompt, duration })` helper
- `lib/vlog.js` refactored to use Kling lipsync
- CLAUDE.md updated — no more HEDRA in stack

**Obsidian vault (source of truth for APIs):**
- `obsidian/blondeshell-brain/APIs/publer.md` — 303 lines
- `obsidian/blondeshell-brain/APIs/fanvue.md` — 254 lines
- `obsidian/blondeshell-brain/APIs/fal.md` — 239 lines (Seedream + Kling i2v + Kling lipsync)
- `obsidian/blondeshell-brain/APIs/_index.md` — index
- Agents load via `import { loadAPIReference } from './lib/obsidian.js'`

**Scripts (new):**
- `scripts/launch_readiness.mjs` → `npm run readiness` — full go/no-go check (41/41 passing)
- `scripts/check_bios.mjs` → `npm run check:bios` — bio consistency
- `scripts/check_fanvue_media.mjs` — diagnostic for media UUID/list
- `scripts/fanvue_check_tokens.js` — verify tokens + /creators/me
- `scripts/test_voice.mjs` → `npm run test:voice` — 3 ElevenLabs clips
- `scripts/test_fanvue_upload.mjs` → `npm run test:fanvue` — E2E multipart upload
- `scripts/test_fanvue_api.mjs` → `npm run test:fanvue-api` — API + token refresh
- `scripts/night_session_batch.mjs` → `npm run schedule:night` — 12 images + QA + schedule Tue/Wed
- `scripts/schedule_monday_v2.mjs` → `npm run schedule:monday` — Monday posts

**Supabase migrations:**
- `supabase/migrations/20260418000001_ab_testing.sql` — ab_test_groups + hook_performance
- `supabase/migrations/20260418000002_brand_arc_seed.sql` — Brand Arc #1 + annual_milestones 2026
- `supabase/migrations/v52_combined.sql` — idempotent catch-all (13 tables + seeds) — RUN VIA SQL EDITOR ✅

**Bug fixes:**
- `lib/publer.js` — fixed result parser (media is direct array, not `{ media: [...] }`)
- `lib/rule_inserter.js` — `glob.sync` instead of named `globSync` (compat with glob v8)
- `scripts/launch_readiness.mjs` — `select('*')` for fanvue_tokens (avoids column-not-found)

---

## ✅ SPRINT 1 RESULTS (Today)

**All 6 steps passed from user's Mac terminal:**
1. `npm run readiness` → 41/41 ✅
2. `npm run test:fal` → 3 images generated ✅
3. `npm run test:voice` → 3 ElevenLabs clips (social/dm/vlog) ✅
4. `npm run test:fanvue-api` → 15 posts + 15 media visible ✅
   `npm run test:fanvue` → upload works, mediaUuid `54b418bb-50ee-4ccc-93e5-e3609d046c94` ✅
5. `npm run schedule:monday` → **6/6 posts scheduled** for Monday (3 gym/home/beach × 2 platforms)
   - Publer IG: `69d763474d92c20853b2f6bd` (ig_business via FB Page)
   - Publer TikTok: `69d76503f9cf2bb87988ea09`
6. `npm run check:bios` → Publer returns empty bio data (API limitation) — user confirmed bios set manually ✅

**Publer scheduled posts (all have job IDs, verified in Publer dashboard):**
- GYM: TT `2026-04-20T21:00Z` + IG `2026-04-20T19:00Z`
- HOME: TT `2026-04-20T14:00Z` + IG `2026-04-20T15:00Z`
- BEACH: TT `2026-04-21T02:00Z` + IG `2026-04-20T23:00Z`

---

## 🔑 ENV STATE (confirmed working from user's .env)

✅ ANTHROPIC_API_KEY, FAL_KEY (NEW key, old had host allowlist), SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY
✅ Publer_API, PUBLER_WORKSPACE_ID
✅ FANVUE_CLIENT_ID, FANVUE_CLIENT_SECRET (tokens in Supabase fanvue_tokens, re-auth'd today)
✅ ELEVENLABS_API_KEY (Voice ID: `briGJOLAce4pTnmxMbbi`)
✅ RESEND_API_KEY, ALERT_EMAILS

❌ Not set (intentional):
- SUBSTY_SERVICE_KEY — no public API, manual or Playwright
- OPENAI_API_KEY — not needed (Claude handles all)
- TWITTER_COOKIES_B64 — not needed (via Publer)
- HEDRA_API_KEY — REMOVED (Kling lipsync replaces it)

---

## 🎯 NEXT STEPS (for new session)

### IMMEDIATE (first action in morning session — Apr 19):
1. **Open `MORNING_QUESTIONS.md`** at repo root — it's the Sprint 2 kickoff checklist:
   - Part A: night-session verification (images/QA/Publer/Monday posts going live)
   - Part B: Sprint 2 decisions (nsfwjs, voice clone, first vlog, batch size)
   - Part C: Supabase + Publer + analytics data to pull
   - Part D: resume-prompt template for the new Claude session

2. **If `npm run schedule:night` not yet run** (sandbox blocks it — must run from Mac):
   - Run from Mac terminal tonight: `npm run schedule:night`
   - In morning, check `/tmp/night_session/results.json`

3. **Delete stale Publer post**: `"I can't write content promoting..."` —
   manual delete via Publer dashboard → Scheduled.

### Sprint 2 (Apr 19) — tomorrow:
- Review Monday posts going live (first real posts!)
- Consider nsfwjs integration to reduce Claude vision QA cost
- Generate 18 images + 6 videos overnight

### Sprint 3 (Apr 20):
- Deploy webhook server to Railway (if not already)
- Activate all crons (currently defined in webhook/server.js):
  - `0 3` revenue_agent
  - `0 4` pipeline
  - `0 6` learning_agent
  - `0 8 Mon` inspiration_engine
  - `0 10` marketing_agent
  - `0 11 1` strategy_agent
  - `0 12 1` tool_eval_agent (monthly)
  - `0 12` coo_agent
  - `0 13` trends_agent
  - `0 15 MWF` vlog_pipeline
  - `0 19` learning_agent evening
  - `0 2 Mon` plan_update_agent
  - `*/30` viral_check (30min, new in v5.2)

### Sprint 6 (Apr 23) — Pre-Launch QA:
- Verify all bios consistent (manually, since Publer doesn't return bio data)
- Full system review

### LAUNCH DAY (Apr 24):
- GO LIVE, first auto-posts
- $150-200 on shoutouts from gaming/meme pages

---

## 📝 IMPORTANT CONTEXT FOR NEXT SESSION

**Sandbox limitation**: Claude Code sandbox environment blocks outbound requests to fal.ai, Fanvue, Publer APIs (403 "Host not in allowlist"). ALL real API tests must be run from user's Mac terminal, not from Claude Code.

**Supabase migration drift**: Remote has ghost migrations `20260411102344/5` not in local git. Fixed by using `supabase/migrations/v52_combined.sql` (idempotent CREATE TABLE IF NOT EXISTS) pasted into SQL Editor instead of `supabase db push`.

**Publer validity warning**: `validity: instagram=false, tiktok=false` on media uploads is a pre-check warning, not blocker. Posts scheduled fine.

**Fanvue tokens storage**: stored in Supabase `fanvue_tokens` table (id='singleton'), NOT in .env. Auth script: `npm run fanvue:auth` opens browser → Railway webhook catches callback → saves to Supabase.

**Key files to reference in new session:**
- `MORNING_QUESTIONS.md` — **START HERE** — Sprint 2 kickoff checklist
- `CLAUDE.md` v5.2 — thin overview (references Obsidian vault)
- `obsidian/blondeshell-brain/APIs/*.md` — full API references
- `package.json` — all npm scripts (38 total after v5.2)
- `/root/.claude/plans/soft-snuggling-lynx.md` — full v5.2 plan + Year 1 roadmap

---

## 🧪 KEY TEST RESULTS FROM TODAY

**Fanvue multipart upload E2E (from user terminal):**
```
[fanvue] Session created — uploadId: 54b418bb-50ee-4ccc-9...
[fanvue] Uploading chunk 1/1 (7004KB)
[fanvue] Processing — uuid: 54b418bb-50ee-4ccc-93e5-e3609d046c94 status: processing
[fanvue] Media status: ready
✅ Upload complete! mediaUuid: 54b418bb-50ee-4ccc-93e5-e3609d046c94
```

**Fanvue API visibility:**
- `/posts?limit=5` → 15 posts visible
- `/media?limit=3` → 15 items visible
- Token refresh flow working

**ffmpeg on user's Mac:** v8.1 installed ✓

---

## 🚨 KNOWN ISSUES / TODOs

1. **Instagram validity=false** — Publer returns this warning but posts are scheduled. Need to verify on Apr 20 that posts actually publish. If they fail, may need to adjust image aspect ratio / size in `lib/image_convert.js`.

2. **Stale failed post in Publer** — `"I can't write content..."` — user needs to delete manually.

3. **Bio API gap** — Publer doesn't return bio content. Future: integrate Instagram Graph API + TikTok API directly for bio checks.

4. **nsfwjs not integrated** — Sprint 2 item, reduces Claude vision QA cost. Low priority, Claude vision works.

5. **deepface not integrated** — Python-only, no good JS equivalent. Using Claude vision for face similarity instead.

---

## 💰 COST TRACKING

Today's spend: ~$0.10 (3 fal.ai images + ElevenLabs credits + Claude)
Monthly budget (Month 1): ~$575-675
Revenue target Month 1: $1,200-2,300 net

---

*Session ended 2026-04-18. Resume: `npm run readiness` to confirm state, then `npm run schedule:night` for night batch.*

---

## 🌙 NIGHT-SESSION RESULTS (Apr 18, run from user's Mac)

- Generated: 12/12
- Approved: 4/12 (beach_playful, gym_athletic, gym_stretching, street_sporty)
- Rejected: 8/12 → 3× NSFW T2, 4× identity <0.85, 1× brand fit
- Scheduled in Publer: **8/8** approved × 2 platforms (Tue+Wed, TT+IG). Twitter was missing from this run — fixed tonight.
- Stale Publer post: NOT visible in schedule — no cleanup needed.

## 🛠️ EOD CODE CHANGES (Apr 18, this session, on branch `claude/push-to-github-PjFnX`)

1. **`scripts/night_session_batch.mjs`** — prompt rewrites, LA hashtags, Twitter integration, IG geo-tag:
   - Added `IDENTITY_CUE` constant → injected into all 12 SETTINGS_MOODS prompts (bright green eyes visible, platinum blonde, no face-covering sunglasses).
   - Rewrote all 12 prompts to remove bikini tops / oversized sleep shirts / sunglasses (T1-safe + brand-aligned).
   - Added `LA_TAGS_TT / LA_TAGS_IG / LA_TAGS_TW` and appended to all captions.
   - Added `CAPTIONS_TWITTER[12]` + Twitter scheduling block (posts go out at `hour+2` UTC, same image as IG/TT).
   - Imports + calls `searchInstagramLocation('Los Angeles, California')` — auto geo-tag every IG post. Falls back to `INSTAGRAM_LA_LOCATION_ID` env var if API search fails.
2. **`lib/publer.js`** — IG location support + search helper:
   - `buildInstagramPhoto` / `buildInstagramVideo` accept `locationId`, emit `details: { location_id }`.
   - `schedulePost({ …, locationId })` threads it through.
   - New `searchInstagramLocation(query, accountId)` — caches results in-memory, tolerant of API shape variance.
3. **`obsidian/blondeshell-brain/APIs/publer.md`** — Twitter row added (new active account). Removed hard "do not post" note.
4. **`MORNING_QUESTIONS.md`** — added A6 (Monday post count mystery), A7 (Twitter status), LOCKED DECISIONS section (B1-B4 answered), Sprint 1 night results summary.

## 🎯 SPRINT 2 PLAN (Apr 19) — locked decisions from owner

### B1. nsfwjs integration — **DO TOMORROW** (day work)
Replace Claude vision in `lib/qa_gate.js` NSFW step with local `nsfwjs` classifier. ~$18/mo savings.
- `npm i nsfwjs @tensorflow/tfjs-node` (add to package.json)
- New `lib/nsfw_classifier.js` with `classifyImage(buffer)` → `{ class, prob }` and T1/T2/T3 gate.
- Wire into `qa_gate.runFullQA` — use nsfwjs for NSFW, KEEP Claude vision for identity + brand fit (no local face-similarity model good enough yet).
- Test: re-run QA on the 8 rejected night-session images, compare verdicts.
- Est: 2-3h.

### B2. Voice — **KEEP** current voice ID `briGJOLAce4pTnmxMbbi`. No action.

### B3. First vlog test (30s) — **YES** (night of Sprint 2)
- `node -e "import('./lib/vlog.js').then(m => m.generateVlog({ arcId: 'arc_001', duration: 30 }))"`
- Validates pipeline: script (Claude) → narration (ElevenLabs) → start frames (Seedream) → i2v clips (Kling v3) → talking head (Kling lipsync) → ffmpeg stitch.
- Cost: ~$0.50. Risk: first real test, likely needs debugging.

### B4. Sprint 2 night batch — **FULL** (18 images + 6 videos + 1 vlog)
- 18 fresh images (6 new settings × 3 moods OR 6 settings × 3 time-of-day). Use the improved prompts from tonight.
- 6 short videos via Kling v3 i2v from approved start frames (standard/image-to-video, 5s each).
- 1× 30s vlog (see B3).
- Schedule Thu-Sat for all 3 platforms (TT + IG + Twitter).
- Cost: ~$3-5 total.

### Other Sprint 2 day tasks (in order)
1. **Morning: verify Monday posts publishing** — check Publer → Published, TikTok, IG. Per A6, investigate why only 3 of 6 Monday posts visible.
2. **nsfwjs integration** (B1, 2-3h).
3. **Re-run `npm run schedule:night`** with tightened prompts if approved count from last night feels thin — expect ~75% approval.
4. **Night: full batch** (B4).

---

*Updated 2026-04-18 EOD. Resume in morning: read MORNING_QUESTIONS.md first, then this file's Sprint 2 section.*
